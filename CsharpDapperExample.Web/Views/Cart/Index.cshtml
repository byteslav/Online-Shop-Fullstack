@model CsharpDapperExample.ViewModels.CartViewModel

<h2 align="center">Shopping Cart</h2>
@{
    if (Model.Products.Any())
    {
        <table class="table">
            <tr>
                <th>Name</th>
                <th>Price</th>
                <th>Description</th>
                <th>Category</th>
                <th></th>
            </tr>
 
            @foreach (var product in Model.Products)
            {
                <tr>
                    <td>@product.Name</td>
                    <td>@product.Price</td>
                    <td>@product.Description</td>
                    <td>@product.Category.Name</td>
                    <td> <a asp-action="RemoveFromCart" asp-route-id="@product.Id">Remove</a></td>
                </tr>
            }
        </table>
    }
    else
    {
        <p>Please, add items to cart.</p>
    }
}